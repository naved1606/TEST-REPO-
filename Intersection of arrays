#Write a function in a Object Orientated Programming language of your choice that 
#takes two sorted arrays of integers as input and returns an array containing numbers common to both arrays without duplicates.


def intersection(arr1. arr2):
    i, j, result=0, 0, []
    while i <len(arr1) and j<len(arr2):
    if arr1[i]==arr2[j]:    
        result.append(arr1[i])
        i=i+1
        j=j+1
    elif arr1[i]<arr2[j]:
        i=i+1
    else 
        j=j+1

    return result

print(intersection([1,2,4,5],[2,4,6,8))

